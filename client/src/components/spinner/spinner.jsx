// 依赖载入
import React, { Component, PropTypes } from 'react';

// 组件载入
import './spinner.scss';

// 组件创建
class Spinner extends Component {

  // 组件静态常量
  static propTypes = {
  };

  // 页面渲染
  render() {
    return (
      <div className={ "" }>

      </div>
    );
  }

  // 组件公共方法
}

export default Spinner;
